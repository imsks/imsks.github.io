import { createGlobalStyle } from 'styled-components';

export const FontFaces = createGlobalStyle`
@font-face {
    font-family: Poppins;
    src: url(url(https://fonts.googleapis.com/css2?family=Poppins&display=swap));
    font-style: normal;
  }
`;
